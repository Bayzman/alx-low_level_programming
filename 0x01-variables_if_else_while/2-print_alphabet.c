#include <stdio.h>

/**
 * main - entry point
 * return - return value
 */

int main()
{
	char alphabet;

	for (alphabet = 'a'; alphabet <= 'z'; alphabet++)
	{
		putchar(alphabet);
	}
	putchar('\n');

	return 0;
}
