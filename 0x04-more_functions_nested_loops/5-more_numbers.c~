#include "main.h"

/**
 * more_numbers - print 0 to 14 ten times
 * Return: void
 */

void more_numbers(void)
{
	int i;
	int c;

	i = 0;
	for (i = 0; i < 10; i++)
	{
		c = 48;
		while (c < 15)
		{
			if (c > 58)
				_putchar(1 + c);
			_putchar(c);
			c++;
		}
		_putchar('\n');
	}
}
